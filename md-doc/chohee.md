Updated At: 2026-03-20 KST

# K-Culture Hub Unified Writing Command

이 문서는 [SITE_NAME] 프로젝트의 포스팅 단일 운영 문서다.
허브 글 작성, 하위 글 작성, 기존 글 수정, 이미지 alt 보강, 내부 링크 점검, 키워드 분리, frontmatter 설계, 품질 검수까지 모두 이 문서를 기준으로 실행한다.

수익 구조: Google AdSense (노출 기반 광고 수익)
목표: 체류 시간 확보, 페이지뷰 극대화, 검색 상단 노출

---

## 0. Start Here

포스팅 작업을 시작할 때는 아래 순서만 먼저 본다.

1. `## 0. Start Here`
2. `## 0-A. Critical Constraints`
3. `## 0-1. Next Writing Targets`
4. `## 0-2. Completion Update Rules`
5. `## 5. Frontmatter 규칙`
6. `## 7. Google SEO 규칙`
7. `## 15. 품질 게이트`

작업 중 판단이 흔들리면:
- 다음에 무엇을 써야 하는지: `## 0-1. Next Writing Targets`
- 완료 후 무엇을 갱신해야 하는지: `## 0-2. Completion Update Rules`
- 제목, 키워드, 본문 SEO 기준: `## 7. Google SEO 규칙`
- 형식 검수: `## 5. Frontmatter 규칙`, `## 15. 품질 게이트`

---

## 0-A. Critical Constraints

아래 규칙은 가장 우선순위가 높은 필수 규칙이다.
하나라도 누락되면 포스팅 미완료로 간주한다.

### [C1] Frontmatter Required

[DO]
- 모든 포스트는 `.mdx` 파일로 작성한다
- `slug`, `lang`, `category`, `title`, `description`, `publishedAt`, `readTime`, `headerGradient`, `pullQuote`, `hreflangSlug`를 반드시 채운다
- 파일명과 `slug`는 반드시 같아야 한다
- KO/EN 짝 파일은 같은 `hreflangSlug`와 같은 `publishedAt`를 사용한다

[NOT]
- 필수 frontmatter 누락 금지
- 파일명과 `slug` 불일치 금지

### [C2] Main Keyword Placement

[DO]
- 제목에 메인 키워드를 반드시 1회 포함한다
- 첫 문단에 메인 키워드 1회와 보조 키워드 1개 이상을 자연스럽게 포함한다
- H2 최소 1개 이상에 메인 키워드 또는 보조 키워드를 반영한다

[NOT]
- 메인 키워드 4회 초과 반복 금지
- 키워드만 나열하는 문장 금지
- 검색 의도 없는 추상 키워드 사용 금지

### [C3] Hub Backlink Required

[DO]
- 하위 글은 본문 어딘가에 허브 글로 가는 내부 링크를 반드시 포함한다
- 허브 링크는 자연스러운 설명형 앵커 텍스트로 연결한다

[NOT]
- Quick Summary나 Related panel에만 허브 링크를 두고 본문에서 누락하는 것 금지
- `여기`, `링크`, `click here` 같은 빈약한 앵커 텍스트 금지

### [C4] Locale-Isolated Internal Links

[DO]
- KO 글은 `/ko/hub/[slug]` 경로만 사용한다
- EN 글은 `/en/hub/[slug]` 경로만 사용한다
- 내부 링크는 실제 존재하는 글로만 연결한다

[NOT]
- KO 본문에서 EN 링크 사용 금지
- EN 본문에서 KO 링크 사용 금지
- 존재하지 않는 글로의 dead link 금지

### [C5] Completion Tracking Update

[DO]
- KO/EN 짝 글이 모두 끝난 뒤 `md-doc/kculture-blog-topics.md` 상태를 갱신한다
- 완료된 항목은 원래 형식을 유지한 채 `Sub [ ]`를 `Sub [v]`, `Hub`를 `Hub [v]`로 바꾼다
- 완료 후 `## 18. 현재 작업 상태`와 `## 19. 진행 로그`도 함께 업데이트한다

[NOT]
- KO 또는 EN 한쪽만 완료된 상태에서 완료 체크 금지
- 원래 제목이나 번호 구조를 바꾸면서 체크하는 것 금지

### [C6] Structure, Fact Box, Internal Links

[DO]
- 음식형, 장소형, 방문형 글은 본문 초반 H2 안에 실용 정보 bullet list를 반드시 넣는다
- 한 글 안에서 H2별 문단 수와 길이를 반드시 다르게 설계한다
- 2문단 고정 패턴은 구조 위반으로 보고, 초안 단계에서 반드시 깨뜨린다
- 내부 링크는 페이지뷰를 높이는 핵심 수단이므로 본문 중간에 관련 글 링크를 자연스럽게 1~2회 삽입한다
- 내부 링크는 글의 30~70% 구간에서 문맥에 맞게 배치한다

[NOT]
- 모든 H2 아래를 2문단 고정 패턴으로 반복 금지
- 글 전체가 사실상 같은 문단 수 반복으로 읽히는 구조 금지
- 실용 정보 블록 생략 금지
- 내부 링크 없이 글을 끝내는 것 금지

### [C7] Image Insertion and Reuse Ban

[DO]
- 글 작성 단계에서는 이미지 없이 본문, SEO, 내부 링크, alt 초안까지만 처리한다
- 본문 이미지 삽입은 항상 사용자가 마지막 단계에서 직접 처리한다

[NOT]
- Codex가 본문 이미지 마크업을 넣는 행위 절대 금지
- 프로젝트 최상위 폴더와 모든 하위 폴더를 포함한 저장소 전체에서 기존에 존재하는 어떤 사진 파일도 글 작성용으로 사용하는 것 절대 금지
- 이전 포스팅에서 사용한 사진을 새 포스팅에 재사용하는 것 절대 금지
- 허브 글과 하위 글 사이에서 동일 사진을 재사용하는 것 절대 금지
- 같은 포스팅 안에 동일한 사진 파일을 두 번 이상 넣는 것 절대 금지

예외:
- 사용자가 현재 글의 이미지 삽입을 위해 직접 추가한 파일은 위 금지 대상에서 제외한다

### [C8] AdSense Content Quality

[DO]
- 글 하나당 본문은 최소 600자 이상 (KO 기준)으로 작성한다
- 모든 문단은 고유한 정보 목적을 가져야 한다
- 광고주 친화적 주제만 다룬다 (음식, 뷰티, 여행은 기본 허용)

[NOT]
- 얇은 콘텐츠 (thin content) 작성 금지
- 같은 의미를 다른 말로 반복해 분량만 늘리는 것 금지
- 광고 정책에 위배되는 주제 (논쟁적 정치, 혐오, 의료 과장 등) 금지

---

## 0-1. Next Writing Targets

현재 바로 써야 할 다음 글:

1. `[001] Hub [ ]`

작성 순서 원칙:
- 허브를 먼저 쓴다
- 그 다음 같은 허브의 하위 글을 쓴다
- KO/EN은 반드시 짝으로 완료한다
- 완료 체크는 KO/EN 짝 파일이 모두 끝난 뒤에만 한다

---

## 0-2. Completion Update Rules

글 1쌍을 완료한 뒤 마지막에 반드시 아래를 한다.

1. 생성된 KO/EN 파일 확인
2. frontmatter 확인
3. 내부 링크와 related panel 확인
4. 이미지 경로와 alt 확인
5. `## 15. 품질 게이트` 점검
6. `md-doc/kculture-blog-topics.md` 상태 업데이트
7. `## 18. 현재 작업 상태`와 `## 19. 진행 로그` 업데이트

`md-doc/kculture-blog-topics.md` 업데이트 규칙:
- `Sub [ ]`는 완료 시 `Sub [v]`로 바꾼다
- `Hub`는 완료 시 `Hub [v]`로 바꾼다
- 원래 문장 구조는 유지하고 상태 표기만 수정한다
- KO/EN 짝 파일이 모두 완료되기 전에는 체크하지 않는다

---

## 1. 목적

이 프로젝트의 우선순위는 아래 순서다.

1. 실제 검색 의도를 반영한 SEO 구조
2. 허브와 하위 글의 키워드 분리
3. 클릭 가능한 제목과 메타 설명
4. 과장 없는 본문 품질과 읽기 쉬운 문단 구성
5. 이미지 alt와 내부 링크 정리
6. 내부 링크 중심의 체류 시간 및 페이지뷰 확보 (AdSense 수익 직결)

한 줄 원칙:

- 검색 상단을 노리되 키워드 나열형 저품질 문서는 만들지 않는다.
- 기존 글 수정 시에는 갈아엎지 않고, 메타·첫 문단·H2·alt 중심으로 최소 수정한다.

---

## 2. 프로젝트 컨텍스트

- Site: `[SITE_URL]` (확정 후 업데이트)
- Routes:
  - KO: `/ko/hub/[slug]`
  - EN: `/en/hub/[slug]`
- Content paths:
  - `content/hub/ko/[slug].mdx`
  - `content/hub/en/[slug].mdx`

카테고리 값 규칙:
- category 값은 반드시 `md-doc/kculture-blog-topics.md`의 해당 항목 카테고리 값을 따른다
- KO/EN 카테고리명은 각 언어에 맞는 페어 값을 그대로 사용한다

카테고리 목록:

| KO | EN |
|----|----|
| `한국 음식 & 맛집` | `Korean Food & Restaurants` |
| `K-뷰티 & 스킨케어` | `K-Beauty & Skincare` |
| `한국 여행 & 명소` | `Korea Travel & Spots` |

파일 경로:
- KO: `content/hub/ko/[slug].mdx`
- EN: `content/hub/en/[slug].mdx`

파일명 규칙:
- slug는 영문 kebab-case만 사용
- 특수문자, 공백, 한글 slug 금지
- 파일명과 `slug` 값은 반드시 동일
- KO/EN 짝 글은 같은 `hreflangSlug`를 사용

---

## 3. 단일 진실 규칙

- 작성/수정 작업 전에는 이 문서를 먼저 읽는다.
- `AGENTS.md`와 충돌하면 이 문서를 우선한다.
- 오래된 예시 제목, 폐기된 프롬프트, 과거 임시 규칙은 따르지 않는다.

---

## 4. 문서 구조 규칙

모든 허브/하위 글은 아래 구조를 따른다.

1. 인트로
2. H2 섹션 (초반 구간 어딘가에 실용 정보 Bullet 필수 포함)
3. H2 섹션 (본문 30~70% 구간 어딘가에 관련 글 내부 링크 1~2회 포함)
4. 빠른 요약 카드
5. 같은 허브 내부 링크 패널
6. 하단 관련 글 패널

카테고리별 초반 실용 정보 block 기준:
- **음식형/맛집형 글**: 위치, 운영시간, 가격대, 예약 여부, 웨이팅, 대표 메뉴 중심으로 설계한다
- **장소형/방문형/촬영형 글**: 위치, 이동, 시간, 추천 동선 중심으로 설계한다
- **뷰티/스킨케어형 글**: 피부 타입, 사용 순서, 선택 기준, 제품 유형 중심으로 설계한다

H2 규칙:
- 모든 H2는 반드시 `## —` 로 시작
- 하이픈 금지
- H2는 검색자 문장처럼 읽혀야 한다
- 설명형 문어체만 반복하지 말고 질문형, 판단형, 선택형 문장을 섞는다
- 키워드를 억지로 끼워 넣은 제목보다 "실제 사용자가 이렇게 검색할까?"에 가까운 문장을 우선한다
- 같은 글 안에서 모든 H2를 같은 어투로 맞추지 않는다

Quick Summary 규칙:
- 일반 H2로 쓰지 않는다
- 카드형 블록으로 유지한다
- 라벨은 `빠른 요약` / `Quick Summary`

Related panel 규칙:
- Quick Summary 아래에 둔다
- KO 패널 기본 라벨:
  - 섹션: `이어서 보면 좋은 글`
  - 허브: `먼저 보기`
  - 다음 하위 글: `다음 글`
  - 추가 하위 글: `같이 보기`

하단 관련 글 패널:
- 광고 수익 구조에서는 외부 서비스 CTA 배너 대신 관련 글 패널을 하단에 유지한다
- 같은 카테고리 또는 같은 허브의 다른 글 2~3개를 연결한다
- 독자가 다음 글로 자연스럽게 이동하도록 유도하는 것이 목적이다

Quick Summary와 Related panel에는 본문 이미지를 넣지 않는다.

---

## 5. Frontmatter 규칙

모든 포스트는 `.mdx` 파일로 작성하고 아래 frontmatter 구조를 따른다.

```mdx
---
slug: "korean-bibimbap-recipe-guide"
lang: "en"
category: "Korean Food & Restaurants"
title: "Korean Bibimbap Guide: What's in It and How to Eat It Right"
description: "Break down what goes into bibimbap, how each ingredient works, and what separates a good bowl from a forgettable one."
publishedAt: "2026-03-20"
readTime: "6 Min Read"
headerGradient: "linear-gradient(135deg, #1a0a2e 0%, #f4258c 100%)"
pullQuote: "the sauce is the decision — everything else is just the setup."
hreflangSlug: "korean-bibimbap-recipe-guide"
---
```

필수 필드:
- `slug`
- `lang`
- `category`
- `title`
- `description`
- `publishedAt`
- `readTime`
- `headerGradient`
- `pullQuote`
- `hreflangSlug`

선택 필드:
- `nextSlug`
- `nextTitle`

필드 규칙:
- `lang`는 `ko` 또는 `en`
- KO category는 해당 토픽의 KO 카테고리명과 일치해야 한다
- EN category는 해당 토픽의 EN 카테고리명과 일치해야 한다
- category 값은 반드시 `md-doc/kculture-blog-topics.md`의 category 정의를 기준으로 한다
- 임의 번역 카테고리명 사용 금지
- `publishedAt`는 ISO 날짜 `YYYY-MM-DD`
- KO/EN 짝 파일은 `publishedAt`를 동일하게 맞춘다
- `title`은 고유해야 하며 60자 이하여야 한다
- `description`은 120~155자여야 한다
- `description` 첫 단어는 `title` 첫 단어와 같으면 안 된다
- `description`은 제목을 풀어쓰기 하지 말고 클릭 이유를 말해야 한다
- `pullQuote`는 기사 안의 한 문장으로, 인용부호 안에서 소문자로 시작한다
- `hreflangSlug`는 KO/EN 짝 글에서 동일해야 한다
- KO `readTime`은 자연스러운 한국어 형식으로 작성한다
- EN `readTime`은 `6 Min Read` 형식을 따른다

---

## 6. 작성 규칙

### 6-1. 인트로 역할

인트로는 3문장 이내로 아래 3가지 역할을 해야 한다.

1. 이 음식/장소/주제를 정의한다
2. 사람들이 단순하게 이해하는 지점을 짚는다
3. 이 글이 무엇을 더 설명하는지 말한다

금지:
- `많은 사람은 X를 Y로 단순화하지만 실제로는`
- `Many people think of X as Y, but in reality`
- `~하는 사람은 보통 X 같지만, 실제로는` 및 모든 변형 — "실제로는"을 포함한 인트로 패턴 전면 금지
- `People often expect X, but` / `People usually think X, but` 변형 포함

같은 허브 안에서는 인트로 유형을 돌려 쓴다.

- A 질문형
- B 숫자/통계형
- C 장면 묘사형
- D 직설형

### 6-2. 본문 문체

- 설명 중심으로 쓴다
- 필요할 때만 리스트 사용
- 장식적인 문장보다 구체적인 장면, 동선, 비교 기준을 우선한다
- 과한 굵게, 과한 불릿, 세일즈 문체 금지

### 6-3. KO/EN 분리 원칙

- KO와 EN은 번역하지 않는다
- 같은 주제를 각 언어 검색 의도에 맞게 다시 쓴다

KO 초점:
- 왜 유명해졌는가
- 한국 사용자 기준 체감 차이
- 지역 비교 또는 시즌별 차이

EN 초점:
- 외국인에게 낯선 배경 설명
- what it is / why it matters / how to read it
- 한국 여행자나 K-컬처 입문자 시선에서 설명

### 6-4. Pattern Diversification Rules

이 프로젝트는 여러 글이 한 묶음으로 발행되므로, 문서군 전체가 공장형 패턴으로 보이지 않게 구조를 분산해야 한다.

필수 원칙:
- 모든 글의 H2 개수를 기계적으로 맞추지 않는다
- 글마다 문단 수와 문단 길이를 다르게 설계한다
- 같은 허브 안에서도 서론 길이, H2 수, 본문 호흡이 똑같아지지 않게 조정한다
- 같은 문장 길이와 같은 전개 리듬을 반복하지 않는다

H2 분산 원칙:
- H2는 글 성격에 따라 유연하게 구성한다
- 일반적으로 3~6개 범위에서 주제에 맞게 선택하되, 모든 글을 같은 개수로 맞추지 않는다
- 허브 글은 넓은 맥락 설명 중심, 하위 글은 구체 의도 해결 중심으로 H2 구성을 다르게 가져간다
- `…이유`, `…구간`, `…방식` 같은 편집 문어체를 기계적으로 반복하지 않는다

문단 분산 원칙:
- 각 H2 아래 문단 수를 동일하게 고정하지 않는다
- 한 글 안에서 H2 아래 문단 수가 전부 같아지면 반드시 깨야 한다
- 2문단 고정 패턴이 3개 이상 연속되면 그 다음 H2는 반드시 1문단 또는 3문단 이상으로 작성한다
- 글 전체를 훑었을 때 "모든 H2가 2문단"처럼 보이면 초안 실패로 간주하고 다시 조정한다

아웃라인 분산 원칙:
- 허브 글과 하위 글은 전개 방식 자체를 다르게 설계한다
- 가능한 전개 유형 예시:
  - 비교형
  - 동선형
  - 해설형
  - 재료/성분형 (음식·뷰티)
  - 시간대형
  - 입문 가이드형
  - 선택 기준형
- 같은 허브 안에서도 글마다 전개 유형을 달리해 반복 패턴을 줄인다

Quick Summary 분산 원칙:
- Quick Summary는 항상 3개 항목을 유지하되, 각 항목의 문장 길이와 리듬은 글마다 다르게 조정한다

금지:
- 모든 글을 같은 H2 개수로 맞추는 것
- 모든 H2 아래를 비슷한 길이의 문단 묶음으로 반복하는 것
- 허브 글과 하위 글이 사실상 같은 아웃라인을 공유하는 것

### 6-5. Practical Info Blocks

음식형, 장소형, 방문형, 뷰티형 글은 본문 초반에 실용 정보 블록을 반드시 넣는다.

카테고리별 포함 항목:

음식형/맛집형:
- `위치`, `운영시간`, `휴무일`, `가격대`, `예약 여부`, `웨이팅 여부`, `대표 메뉴` 중 해당 항목

장소형/방문형:
- `위치`, `가는 법`, `운영시간`, `요금`, `추천 시간대`, `주의사항` 중 해당 항목

뷰티/스킨케어형:
- `피부 타입`, `사용 단계`, `핵심 성분`, `선택 기준` 중 해당 항목

필수 규칙:
- 본문 초반 H2 안에 bullet list 형식으로 배치한다
- 모든 글의 첫 번째 H2에 기계적으로 넣지 않는다
- 모든 글에 같은 항목 조합을 반복하지 않는다

---

## 7. Google SEO 규칙

### 7-1. 메인 키워드

각 글은 언어별로 메인 키워드 1개를 가진다.

조건:
- 실제 검색창에 들어갈 법한 표현
- 추상적 테마 문구 금지
- 철학적, 감상적 표현 금지
- 같은 허브 안 다른 글과 중복 금지
- 가급적 롱테일 검색어 우선

매우 중요:
- `md-doc/kculture-blog-topics.md`의 제목과 메인 키워드는 기획 초안일 뿐 최종 고정값이 아니다
- 실제 글 작성 시에는 더 검색량이 높고 검색 의도가 선명한 실사용 키워드로 다시 설계한다
- 제목은 메인 키워드를 포함하되, 클릭 이유가 보이는 구체 효익이나 비교 포인트를 함께 붙인다

### 7-2. 보조 키워드

각 글은 보조 키워드 3~5개를 가진다.

역할:
- 메인 키워드 보강
- 세부 검색 의도 확장
- H2, 첫 문단, 중간 문단, 이미지 alt, 요약 카드에 분산 반영

### 7-3. 배치 규칙

- `title`: 메인 키워드 포함
- 첫 문단: 메인 키워드 + 보조 키워드 1~2개 포함
- H2 1개 이상: 메인 키워드 또는 보조 키워드 포함
- 본문 중간 문단: 보조 키워드를 자연스럽게 맥락 안에 분산 반영
- 이미지 alt: 장면 설명 중심으로 보조 키워드 1개만 자연스럽게 포함 가능
- 마무리/요약: 메인 키워드 1회 자연스럽게 회수 가능

반복 제한:
- 메인 키워드는 본문에서 3~4회 이하
- 키워드만 나열하는 문장 금지
- 같은 문장 구조로 반복 삽입 금지

### 7-4. 제목과 설명 최적화

- 제목은 검색형 문구를 앞쪽에 두는 것을 우선 검토한다
- 제목에는 반드시 핵심 키워드가 들어가야 한다
- 제목은 과장보다 구체성, 비교성, 효익을 우선한다
- description은 제목 반복이 아니라 클릭 이유를 설명하는 독립 문장이어야 한다

### 7-5. Google SEO 품질 원칙

- E-E-A-T 관점에서 구체 관찰, 현장성, 비교 기준을 넣는다
- 문단마다 정보 목적이 분명해야 한다
- 같은 의미를 다른 표현으로 반복해 길이만 늘리지 않는다
- 허브 글과 하위 글은 서로 다른 검색 의도를 담당하게 설계한다
- 내부 링크는 실제 존재하는 글만 연결한다
- 얇은 콘텐츠, 과장형 문구, 키워드 스터핑을 피한다

---

## 8. 수정 규칙

기존 글을 수정할 때는 아래 순서로만 손댄다.

1. `title`
2. `description`
3. 첫 문단
4. H2 1~2개
5. 이미지 alt
6. 관련 글 패널 문구

기본 원칙:
- 본문을 갈아엎지 않는다
- 기존 논지와 구조는 최대한 유지한다
- 과한 리라이팅 금지

실무 한 줄:
- 제목은 더 검색 친화적으로
- 첫 문단은 키워드 더 선명하게
- H2는 1~2개만 검색형으로
- alt는 짧고 구체적으로

---

## 9. 허브와 하위 글 분리 규칙

허브 글:
- 상위 검색 의도 담당
- 주제 전체의 상징성, 개요, 대표성, 비교 구조를 설명

하위 글:
- 구체 검색 의도 담당
- 특정 재료, 레시피, 제품 유형, 특정 장소, 특정 기법 등 세부 주제 담당

절대 금지:
- 허브가 하위 글 메인 키워드를 빼앗는 것
- 하위 글이 허브의 상위 의도를 다시 넓게 반복하는 것

예시:
- 허브: `한국 스킨케어 루틴 완벽 가이드`
- 하위1: `글라스 스킨 만드는 단계별 루틴`
- 하위2: `토너패드 올바른 사용법`

---

## 10. 내부 링크 규칙

- 모든 하위 글은 본문에 허브 글 역링크를 포함한다
- 본문 30~70% 구간에 관련 글 내부 링크를 1~2개 자연스럽게 삽입한다 (페이지뷰 확보 핵심)
- 허브 글 Related panel에는 실제 존재하는 하위 글만 넣는다
- dead link 금지
- 내부 링크 anchor text는 목적지를 설명하는 자연어로 쓴다
- KO는 `/ko/hub/...`만 사용
- EN은 `/en/hub/...`만 사용
- Cross-language link 금지

허브 완료 후 반드시:
- 허브 related panel 누락 점검
- 하위 글 역링크 점검
- 각 하위 글 related panel 누락 점검
- 키워드 중복 점검

---

## 11. 이미지 및 alt 규칙

### 11-1. 본문 이미지 문법

- 글 작성 단계에서 Codex가 본문 이미지를 넣는 행위는 절대 금지한다
- 이미지 삽입 작업은 항상 사용자만 직접 한다
- Codex는 글 작성, 수정, SEO, 내부 링크, alt 초안까지만 담당한다
- 프로젝트 전체의 기존 사진 파일은 글 작성 과정에서 어떤 형태로도 사용하지 않는다
- 이미지를 넣는 시점에는 각 H2 바로 아래 첫 줄에만 `![alt](/images/...webp)`를 반영한다
- 실제 `.webp` 파일이 준비되기 전에는 깨진 `/images/...` 경로를 본문에 남기지 않는다
- raw HTML `<img>` 금지

### 11-2. 이미지 경로

- 저장 위치: `public/images/hub/[hub-slug]/`
- MDX 경로: `/images/hub/[hub-slug]/[file]-kr.webp` 또는 `-en.webp`
- 최종 경로는 항상 `.webp`
- KO 파일 이미지는 `-kr.webp`, EN 파일 이미지는 `-en.webp`로 끝난다
- 글당 이미지 수는 보통 3~7개를 기준으로 한다
- 이전 포스팅에서 사용한 사진을 새 포스팅에 다시 쓰는 것은 절대 금지한다

### 11-3. alt 규칙

- alt는 비워두지 않는다
- 장면 설명 우선
- 검색 의도 키워드 1개만 자연스럽게 포함 가능
- 짧고 구체적으로 쓴다
- 시적인 표현 금지
- 추상적 감상 금지
- 키워드 나열 금지

---

## 12. 법적/브랜드 안전 규칙

### 12-1. 음식 및 레시피 관련

- 특정 식당의 레시피를 "정통" 또는 "원조"로 단정 짓는 표현 금지
- 건강 효능 과장 금지 (예: "암을 예방한다", "다이어트에 확실하다")
- 의학적 효과로 오인될 수 있는 표현 금지
- 식품 성분이나 알레르기 정보를 단정적으로 서술하지 않는다

### 12-2. 뷰티/스킨케어 관련

- 피부 개선 효과를 의학적 치료처럼 묘사하는 표현 금지
- 특정 브랜드 제품을 특정 인물과 연결하는 표현 금지
- 성분 효능을 과장하거나 단정적으로 서술하는 것 금지
- 패션, 뷰티 브랜드는 일반 정보 맥락에서만 허용

### 12-3. 장소 및 재산 관련

- 사유지, 비공개 구역 무단 접근 묘사 금지
- 공개된 장소명과 지명만 사용
- 특정 인물의 주거지성, 단골 장소성 서술 금지

### 12-4. AdSense 정책 준수

- 논쟁적 정치, 혐오, 성인 콘텐츠 금지
- 광고주 친화적 주제만 다룬다
- 사실 확인 없는 정보를 단정형으로 서술하지 않는다

---

## 13. 내부 링크 중심 체류 전략

AdSense 수익은 페이지뷰와 체류 시간에 직결된다.
외부 서비스 CTA 배너 대신 내부 링크로 독자를 다음 글로 안내하는 것이 핵심이다.

기본 규칙:
- 본문 30~70% 구간에 관련 글 내부 링크 1~2개 자연스럽게 삽입
- 글 하단 Related panel에 같은 허브 또는 같은 카테고리의 글 2~3개 연결
- anchor text는 목적지 글의 주제를 자연스럽게 설명하는 문장으로 작성

금지:
- 모든 H2마다 내부 링크 삽입 (과도한 링크는 사용자 경험 저해)
- `여기를 클릭`, `더 보기` 같은 빈약한 anchor text
- 존재하지 않는 글로의 dead link
- Cross-language link

---

## 14. 세션 운영 규칙

- 기본 세션 단위: KO 3개 + EN 3개 또는 KO/EN 1쌍
- 각 KO 글은 같은 `hreflangSlug`의 EN 글과 짝을 이뤄야 한다
- 작업 전 현재 진행 상태와 진행 로그를 확인한다
- 작업 후 진행 로그를 업데이트한다
- 포스팅 완료 후 `md-doc/kculture-blog-topics.md`에서 해당 주제의 완료 표시를 갱신한다
- KO/EN 짝 글이 모두 완료된 뒤에만 `md-doc/kculture-blog-topics.md`를 완료 처리한다

우선순위:
1. Hub articles
2. Sub-articles linked to each hub
3. Remaining topics from `md-doc/kculture-blog-topics.md`

---

## 15. 품질 게이트

최종 제출 전 반드시 점검:

콘텐츠:
- title 60자 이하
- description 120~155자
- description이 title 반복이 아닌가
- 인트로 3역할 충족
- 같은 허브 내 인트로 유형 중복 없는가
- 패딩 문장 없는가
- 본문 600자 이상인가 (AdSense thin content 방지)

키워드:
- 메인 키워드가 title과 첫 문단에 있는가
- 보조 키워드가 본문에 맥락상 자연스럽게 분산되어 있는가
- 같은 허브 안 다른 글과 키워드 겹치지 않는가

구조:
- H2 아래 문단 수가 전부 같지 않은가
- 2문단 고정 패턴이 글 전체를 지배하지 않는가
- H2 문장이 검색자 문장처럼 자연스럽게 읽히는가
- 음식형/장소형 글이라면 초반 실용 정보 bullet list가 있는가

이미지:
- body image alt 비어 있지 않은가
- alt가 짧고 구체적인가
- `.webp` 경로인가

링크:
- 하위 글이면 허브 역링크가 본문에 있는가
- dead link 없는가
- 언어 혼합 링크 없는가
- 본문 30~70% 구간에 관련 글 내부 링크 1~2개 있는가
- 하단 Related panel에 관련 글 2~3개 연결됐는가

법적:
- 음식/뷰티 효능 과장 표현이 없는가
- 특정 브랜드와 인물을 연결하는 표현이 없는가
- AdSense 정책 위배 표현이 없는가

---

## 16. 문자 수 검증

수정 후 반드시 title과 description의 정확한 문자 수를 센다.

보고 형식:

```text
title: [actual text] -> [N]자
description: [actual text] -> [N]자
```

추정 금지.

---

## 17. 현재 허브 키워드 맵

### 완료 또는 진행 중 허브

| 허브 | 허브 메인 키워드(KO) | 허브 메인 키워드(EN) | 상태 |
|------|----------------------|----------------------|------|
| [001] 한국 길거리 음식 가이드 | 한국 길거리 음식 추천 | Korean street food guide | 진행 중 |
| [002] 한국 스킨케어 루틴 가이드 | 한국 스킨케어 루틴 순서 | Korean skincare routine guide | 미시작 |
| [003] 서울 여행 필수 코스 | 서울 여행 필수 코스 | Seoul travel itinerary | 미시작 |

---

## 18. 현재 작업 상태

- Hub [001] 한국 길거리 음식 KO/EN 완료
- Sub [001-1] 서울 떡볶이 맛집 추천 KO/EN 완료
- Sub [001-2] 명동 길거리 음식 지도 KO/EN 완료

다음 우선순위:
1. Sub [001-3] 한국 핫도그 종류 KO/EN 작성
2. [002] Hub 한국 스킨케어 루틴 작성

---

## 19. 진행 로그

이 섹션은 이후 세션마다 계속 누적한다.

### 2026-03-20 프로젝트 시작

- 커맨드 문서 초안 작성 완료
- 사이트 URL 미확정 → 확정 시 섹션 2 업데이트 필요
- 토픽 파일: `md-doc/kculture-blog-topics.md` 생성 완료

### 2026-03-20 [001] Hub + Sub 1차 배치 완료

- `md-doc/kculture-blog-topics.md` 생성 (3개 허브, 9개 서브 기획)
- Hub [001] `korean-street-food-guide` KO/EN 작성 완료
- Sub [001-1] `tteokbokki-best-spots-seoul` KO/EN 작성 완료
- Sub [001-2] `myeongdong-street-food-map` KO/EN 작성 완료
- 내부 링크 허브↔서브 양방향 연결 확인
- 이미지 삽입은 사용자 직접 처리 예정

---

## 20. 허브 완료 후 필수 점검

허브 + 해당 하위 글이 모두 끝났다면 아래를 먼저 한다.

1. 허브/하위 글 전부 읽기
2. related panel 누락 확인
3. 하위 글 역링크 확인
4. dead link 제거
5. 허브 내 키워드 중복 목록 점검
6. 겹치면 더 구체적인 롱테일로 수정
7. 필요 시 nextSlug / nextTitle 정리
8. 완료된 글이 `md-doc/kculture-blog-topics.md`에 정확히 반영됐는지 확인

---

## 21. 수정 작업용 한 줄 프롬프트

기존 글 수정 시에는 아래 기준으로 본다.

- 메인 키워드는 유지
- title은 더 검색 친화적으로 미세 조정
- description은 클릭 이유 중심으로 재작성
- 첫 문단에 메인 키워드와 보조 키워드 1~2개 반영
- H2는 1~2개만 더 검색형으로 조정
- alt는 짧고 구체적으로 보정
- 본문 나머지 구조와 논지는 유지
